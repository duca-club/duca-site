---
/**
 * Counter Component
 *
 * @description A component that displays a number with a title and subtitle
 */
interface Props {
  /**
   * The number to display
   */
  count: string | number
  /**
   * The title text to display below the count
   */
  title: string
  /**
   * The subtitle text to display below the title
   */
  sub: string
  /**
   * An id for the counter to update it
   */
  count_id?: string
}

const { count, title, sub, count_id } = Astro.props
---

<div class="animate text-center">
  <p class="text-6xl font-bold" id={count_id}>{count}</p>
  <p class="text-4xl font-semibold">{title}</p>
  <p class="text-2xl">{sub}</p>
</div>

<style lang="scss">
  div > p:first-child {
    color: var(--link-color);
  }
</style>
